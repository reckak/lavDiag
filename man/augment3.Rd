% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agument4.R, R/augment3.R
\name{augment3}
\alias{augment3}
\title{Augment ordinal SEM with factor scores, original data, and selected predictions}
\usage{
augment3(fit, prefix_ystar = ".ystar_")

augment3(fit, prefix_ystar = ".ystar_")
}
\arguments{
\item{fit}{A fitted lavaan object intended for ordinal (ordered-categorical) models.}

\item{prefix_ystar}{Prefix for latent predicted values (default ".ystar_").}

\item{what}{Character vector; subset of c("star","prob","exp","class"). Default "exp".}
}
\value{
A tibble with factor scores, original indicators, and requested predictions.

A tibble containing factor scores, original indicators (if present in appended data),
\verb{.ystar_<item>}, and \verb{.pr_<level>__<item>} columns.
}
\description{
Works for single- and multi-group lavaan models with \strong{only ordinal indicators}.
Select what to compute via \code{what}:
\itemize{
\item "star"  : latent predictor y*_j = nu_j + lambda_j• \%*\% eta  (scaled to unit-variance if theta)
\item "prob"  : per-category probabilities  .pr_\if{html}{\out{<level>}}__\if{html}{\out{<item>}}
\item "exp"   : expected score on the ordinal scale  .yexp_\if{html}{\out{<item>}}  (coded 1..m)
\item "class" : most likely category  .class_\if{html}{\out{<item>}}
}

Works for single- and multi-group lavaan models with \strong{only ordinal indicators}.
For each ordinal indicator j, computes:
\itemize{
\item latent predictor:         y*_j = nu_j + lambda_j• \%*\% eta   (scaled to unit-variance if theta)
\item category probabilities:   .pr_\if{html}{\out{<level>}}__\if{html}{\out{<item>}}  (per observed or inferred level)
}
}
